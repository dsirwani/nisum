This app is created using the React Library.

To run the App - just take the code on your local machine and run the command
    npm start

  The app will start running on local server : localhost:3000